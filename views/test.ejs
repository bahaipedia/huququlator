<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Test Debug Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .section {
            margin-bottom: 20px;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f9f9f9;
        }
        .section h2 {
            margin-top: 0;
            color: #333;
        }
        pre {
            background-color: #eee;
            padding: 10px;
            border-radius: 5px;
            overflow-x: auto;
        }
    </style>
</head>
<body>
    <h1>Test Debug Page</h1>

    <div class="section">
        <h2>1. Input Parameters</h2>
        <pre><%= JSON.stringify(debug.params, null, 2) %></pre>
    </div>

    <div class="section">
        <h2>2. SQL Query and Parameters</h2>
        <p><strong>Query:</strong></p>
        <pre><%= debug.query %></pre>
        <p><strong>Parameters:</strong></p>
        <pre><%= JSON.stringify(debug.queryParams, null, 2) %></pre>
    </div>

    <div class="section">
        <h2>3. Raw Transactions from Database</h2>
        <% if (debug.transactions.length > 0) { %>
            <pre><%= JSON.stringify(debug.transactions, null, 2) %></pre>
        <% } else { %>
            <p>No transactions fetched.</p>
        <% } %>
    </div>

    <div class="section">
        <h2>4. Error (if any)</h2>
        <% if (debug.error) { %>
            <pre><%= debug.error %></pre>
        <% } else { %>
            <p>No errors encountered.</p>
        <% } %>
    </div>
</body>
</html>
